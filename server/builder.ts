// write functions to build the application in various mode (dev, prod, test)
// in dev mode ui and server are running should be running in parallel
// in prod mode only server should be running
// in test mode only server should be running
//
// Path: server\builder.ts

export default function builder() {

}
